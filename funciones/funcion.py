def suma(a, b, c): # son variables donde tenemos que asignarle un valor
    total = a + b * c 
    # creamos otra varible para tomarlo de referencia para que nos sea retornado.
    return total

resultado = suma(4, 12, 23)
print(resultado) # para ver el resultado por pantalla tengo que guardar dicho valor en una variable